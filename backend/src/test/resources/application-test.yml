# Used with @ActiveProfiles("test") in integration tests. Datasource comes from Testcontainers @ServiceConnection.
spring:
  config:
    activate:
      on-profile: test

planted:
  openai:
    api-key: test-dummy-key
  weather:
    enabled: false
  outdoor-reminder-refresh:
    enabled: false
