---
description: Frontend Specialist — Next.js App Router patterns, async UX rules, TanStack Query usage, and typed API client standards. Activates on frontend TypeScript files.
globs: frontend/**/*.{ts,tsx}
alwaysApply: false
---

# Frontend Specialist

## Stack
Next.js **16** (App Router), TypeScript, Tailwind CSS, shadcn/ui, TanStack Query (React Query), REST for core CRUD/upload/status flows, Vercel AI SDK available for future agent/chat features.

## Dev proxy and LAN
- **`src/lib/api.ts`:** In development, default to **same-origin** `/api` (empty base) so phones on the LAN use the Next host; **`next.config.ts`** rewrites `/api` and `/images` to Spring (`PLANTED_API_PROXY_ORIGIN`, default `http://127.0.0.1:8080`).
- **`allowedDevOrigins`:** Auto-filled from private IPv4 at dev startup; **`PLANTED_DEV_EXTRA_ORIGINS`** for extras.
- Image URLs may be **root-relative** `/images/...`; components must not assume every app image URL starts with `http`.

## App Router Routes
- `/plants` — PlantList page
- `/plants/upload` — PlantUploadForm page
- `/plants/[id]` — PlantDetail page
- `/plants/[id]/pruning` — PruningFlow page
- `/screensaver` — ScreenSaverCarousel page

## Required Components
`PlantUploadForm`, `PlantList`, `PlantListItemCard`, `ReminderIconRow`, `PlantHeroImage`, `PlantImageStrip`, `PlantStatusCard`, `PlantAnalysisResultCard`, `CareTopicAccordion`, `CareInstructionPanel`, `FunFactsPanel`, `PruningUploadForm`, `ScreenSaverCarousel`

## Async UX Rules (critical)
- Never leave the user with unexplained loading
- Always distinguish: `PENDING` / `PROCESSING` / `COMPLETED` / `FAILED`
- Poll detail pages while any analysis status is PENDING or PROCESSING **or while any bio section is refreshing** (`refetchInterval: hasRefreshingBioSections(data) ? 3000 : false`)
- Keep list views fast — driven from persisted `PlantReminderState`, never fresh model calls
- Use optimistic UI only when safe and reversible

## Plant bio sections on the client
The detail page reads per-section cached data from `plant.bioSections` (see `PlantDetailResponse.bioSections` in `src/types/plant.ts`). Always access it via the helpers in **`src/lib/bioSections.ts`** — never reach into `bioSections` directly from a component.

- `getSpeciesIdSection(plant)`, `getHealthSection(plant)`, `getSpeciesDescriptionSection(plant)`, and one helper per care topic return a `BioSectionView` with `content`, `status`, and `isRefreshing`.
- **`flattenBioSectionsToAnalysis(plant)`** projects bio section content into the legacy `AnalysisSummaryDto` shape so existing UI (care accordions, species panel) keeps working while backend backfill proceeds. Prefer this over raw `plant.latestAnalysis`.
- **`hasRefreshingBioSections(plant)`** is the signal for detail-page `refetchInterval`. Care accordions and the species/health panels should forward a `refreshing` prop and show a subtle "Updating…" hint (never a blocking spinner) when that section is still regenerating.
- New plants may have all sections refreshing simultaneously on first view — render per-section skeletons ("Identifying species…", "Species profile is being prepared…", "Checking health…") rather than one global spinner.

## TanStack Query Patterns
- `useQuery` for list and detail reads with appropriate `staleTime`
- `useQuery` with `refetchInterval` on detail page while jobs are incomplete
- `useMutation` for upload, recording events, archive/restore
- Invalidate relevant queries after mutations

## List Page Image Rules
- The `PlantListItemCard` thumbnail priority: `illustratedImage` → `originalImage` (user's uploaded photo) → Sprout placeholder
- Never use `healthyReferenceImage` as a list thumbnail — reference images are internet sourced and impersonal; only the user's own uploads appear in list cards

## Technical Standards
- Keep API integrations isolated in `src/lib/api.ts` — typed functions per endpoint
- Use shared types in `src/types/plant.ts` mirroring backend DTOs exactly
- Build reusable, composable components
- Handle empty / error / loading states thoroughly in every component
- Keep accessibility in mind (aria labels, keyboard navigation)
- Do NOT call OpenAI directly from frontend for core app logic
- Do NOT duplicate backend business rules in frontend code
- Design with future Vercel AI SDK agent/chat affordances in mind, but do not force chat into flows that work better as standard UI

## Collaboration Rules
- Treat the Spring backend as source of truth for contracts
- Do not assume synchronous model completion — always handle async state
